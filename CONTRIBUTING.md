# Contributing

Thanks for your interest in contributing to EnduranceBloc. Please follow these steps:

- Fork the repo and create a branch from `dev`.
- Run `npm install` and `npm run dev` to test locally.
- Open a PR against `dev` and include tests or screenshots if applicable.
- Follow code style and run `npm run lint` and `npm run format` before submitting.
